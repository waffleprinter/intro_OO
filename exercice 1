"""
Jacques-Conrad Calagui-Painchaud, 405
TP4 - exercice 1
Programme qui fait un objet "StringFoo" avec un attribut "message". Permet l'usager de redefinir cette message
et de le printer.
"""


class StringFoo:
    message = ""

    def set_string(self, string):
        """
        Fait en sorte que l'attribut message devient un string que l'usager chosi.

        :param string: Le string que l'usager choisi
        :return: None
        """
        self.message = string

    def print_string(self):
        """
        Print l'attribut message tout en lettre majuscule.

        :return: None
        """
        print(self.message.upper())


string_foo = StringFoo()
string_foo.set_string("waffleprinter")
string_foo.print_string()
